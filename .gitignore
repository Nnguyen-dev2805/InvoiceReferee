# Local tooling (intentionally ignored)
.agents
AGENTS.md
.codegraph
.claude

# Python bytecode / caches
__pycache__/
*.py[cod]
*$py.class

# Packaging / build artifacts
build/
dist/
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/
env/
ENV/

# Test / coverage / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/

# Secrets / local config
.env
.env.*
!.env.example
# Streamlit deploy secrets (keep only the template committed)
.streamlit/secrets.toml

# OCR model / runtime caches (weights are downloaded/warmed, never committed)
.paddlex/
.paddlex_cache/
.ocr_home/

# OS / editor cruft
.DS_Store
